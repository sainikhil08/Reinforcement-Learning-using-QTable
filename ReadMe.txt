In the training phase, I ran the model with 10 episodes each with 50 steps.

rewards.txt file shows the reward results for each episode in training phase.


In testing Phase, I ran the model for 10 episodes with 20 steps in each episode.
first test run is based on random actions without any Q-table
Second test run is based on Q-learning with Q-tale

test_results file shows results for each episode whether its well mixed or not and its reward.

instructions:

Run the exec_environment file, it will start training the model then executes
test_agent to test the model